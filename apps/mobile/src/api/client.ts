import type { ApiEnvelope } from "@/types/auth";
const base = process.env.EXPO_PUBLIC_API_BASE_URL?.replace(/\/$/, "");
export class ApiError extends Error { constructor(public code?: string, public details?: ApiEnvelope<never>["details"], message = "Something went wrong") { super(message); } }
export async function request<T>(path: string, method: "GET" | "POST" | "PATCH", body?: unknown, accessToken?: string, headers?: Record<string,string>): Promise<ApiEnvelope<T>> { if (!base) throw new ApiError("API_CONFIGURATION_ERROR", undefined, "Set EXPO_PUBLIC_API_BASE_URL before continuing."); const response = await fetch(`${base}${path}`, { method, headers: { "content-type": "application/json", accept: "application/json", ...(accessToken ? { authorization: `Bearer ${accessToken}` } : {}), ...headers }, body: body === undefined ? undefined : JSON.stringify(body) }); const payload = await response.json().catch(() => ({ success: false, message: "Unexpected server response" })) as ApiEnvelope<T>; if (!response.ok) throw new ApiError(payload.code, payload.details, payload.message); return payload; }
export const post = <T>(path: string, body: unknown, headers?: Record<string,string>) => request<T>(path, "POST", body, undefined, headers);
export const getAuthenticated = <T>(path: string, accessToken: string) => request<T>(path, "GET", undefined, accessToken);
export const patchAuthenticated = <T>(path: string, body: unknown, accessToken: string) => request<T>(path, "PATCH", body, accessToken);
