import { zodResolver } from "@hookform/resolvers/zod";
import { useRouter } from "expo-router";
import { Controller, useForm } from "react-hook-form";
import { useEffect, useRef, useState } from "react";
import { ActivityIndicator, Pressable, StyleSheet, Text, TextInput, View } from "react-native";
import { ApiError, post } from "@/api/client";
import { CUSTOMER_ANALYTICS_DISTINCT_ID_HEADER, customerAnonymousAnalyticsId, trackCustomerEvent, trackCustomerEventOnce } from "@/analytics/customer";
import { BrandHeader, FormError, Heading, PasswordChecklist, PrimaryButton, Screen, TextField } from "@/components/ui";
import { forgotPasswordSchema, loginSchema, resetPasswordSchema, type ForgotPasswordValues, type LoginValues, type ResetPasswordValues } from "@/schemas/customer-auth";
import { useAuthReturn, useCustomerSession, useResetFlow } from "@/store/auth-flow";
import { isSafeMarketplaceReturnPath } from "@/marketplace/filters";
import { colors, radii } from "@/theme/tokens";
import type { CustomerSession, NextAction } from "@/types/auth";

const errorMessage=(error:unknown,fallback:string)=>{const api=error as ApiError;return ({INVALID_CREDENTIALS:"Incorrect email/phone or password.",LOGIN_RATE_LIMITED:"Too many login attempts. Please try again later.",ACCOUNT_VERIFICATION_REQUIRED:"Please verify your email before signing in.",ACCOUNT_SUSPENDED:"This account is suspended.",ACCOUNT_LOCKED:"This account is locked.",INVALID_OTP:"That code was not right. Check your email and try again.",OTP_EXPIRED:"That code has expired. Request a new one.",OTP_ATTEMPTS_EXCEEDED:"Too many attempts. Request a new code to continue.",OTP_NO_LONGER_VALID:"That code is no longer valid. Request a new code to continue."}[api.code??""]??api.message??fallback);};
const routeFor=(action:NextAction)=>({VERIFY_EMAIL:"/verify-email",COMPLETE_BUYER_ONBOARDING:"/buyer",COMPLETE_SELLER_ONBOARDING:"/seller",OPEN_BUYER_DASHBOARD:"/marketplace",OPEN_SELLER_DASHBOARD:"/seller/listings"}[action]);

export function LoginScreen(){const router=useRouter();const{establishSession}=useCustomerSession();const{returnTo,setReturnTo}=useAuthReturn();const{control,handleSubmit,formState:{errors,isSubmitting},setError}=useForm<LoginValues>({resolver:zodResolver(loginSchema),defaultValues:{identifier:"",password:""}});const submit=async(values:LoginValues)=>{try{void trackCustomerEvent("Login Submitted",{login_identifier_type:values.identifier.includes("@")?"email":"phone"});const anonymousId=await customerAnonymousAnalyticsId();const result=await post<CustomerSession>("/auth/login",values,anonymousId?{[CUSTOMER_ANALYTICS_DISTINCT_ID_HEADER]:anonymousId}:undefined);if(!result.data)throw new Error("Session was not returned");await establishSession(result.data);const canReturn=result.data.nextAction==="OPEN_BUYER_DASHBOARD"||result.data.nextAction==="OPEN_SELLER_DASHBOARD";const destination=canReturn&&isSafeMarketplaceReturnPath(returnTo)?returnTo:routeFor(result.data.nextAction);if(destination===returnTo)setReturnTo(null);router.replace(destination);}catch(error){setError("root",{message:errorMessage(error,"Unable to sign in.")});}};return <Screen><BrandHeader/><Heading title="Login back to your account" copy="Log in to save homes, message agents and manage your listings."/><Controller control={control} name="identifier" render={({field})=><TextField label="Email address / Phone" keyboardType="email-address" autoComplete="email" value={field.value} onChangeText={field.onChange} error={errors.identifier?.message}/>}/><Controller control={control} name="password" render={({field})=><TextField label="Password" secure autoComplete="password" value={field.value} onChangeText={field.onChange} error={errors.password?.message}/>}/><FormError>{errors.root?.message}</FormError><Pressable accessibilityRole="button" onPress={()=>router.push("/forgot-password")}><Text style={styles.link}>Forgot Password?</Text></Pressable><PrimaryButton title="Log in" loading={isSubmitting} onPress={handleSubmit(submit)}/></Screen>}

export function ForgotPasswordScreen(){const router=useRouter();const{setResetFlow}=useResetFlow();const{control,handleSubmit,formState:{errors,isSubmitting},setError}=useForm<ForgotPasswordValues>({resolver:zodResolver(forgotPasswordSchema),defaultValues:{email:""}});const submit=async(values:ForgotPasswordValues)=>{try{void trackCustomerEvent("Forgot Password Requested",{});const result=await post<{resendAvailableIn:number}>("/auth/forgot-password",values);setResetFlow({email:values.email,resendAvailableIn:result.data?.resendAvailableIn??60});router.push("/verify-reset-otp");}catch(error){setError("root",{message:errorMessage(error,"Unable to start password recovery.")});}};return <Screen><BrandHeader/><Heading title="Forgot password?" copy="Enter your email address and we&apos;ll send a password reset code."/><Controller control={control} name="email" render={({field})=><TextField label="Email address" keyboardType="email-address" autoComplete="email" value={field.value} onChangeText={field.onChange} error={errors.email?.message}/>}/><FormError>{errors.root?.message}</FormError><PrimaryButton title="Send reset code" loading={isSubmitting} onPress={handleSubmit(submit)}/></Screen>}

export function ResetOtpScreen(){const router=useRouter();const{resetFlow,setResetFlow}=useResetFlow();const[digits,setDigits]=useState(["","","","","",""]);const[busy,setBusy]=useState(false);const[message,setMessage]=useState("");const[remaining,setRemaining]=useState(resetFlow?.resendAvailableIn??0);const[resends,setResends]=useState(0);const[attempts,setAttempts]=useState(0);const refs=useRef<Array<TextInput|null>>([]);useEffect(()=>{if(!resetFlow)router.replace("/forgot-password");else void trackCustomerEventOnce("verification-forgot-password","Verification Screen Viewed",{otp_context:"forgot_password"});},[resetFlow,router]);useEffect(()=>{if(!remaining)return;const timer=setTimeout(()=>setRemaining(value=>Math.max(0,value-1)),1000);return()=>clearTimeout(timer);},[remaining]);const submit=async(code=digits.join(""))=>{if(!resetFlow||code.length!==6||busy)return;setBusy(true);setMessage("");try{const result=await post<{resetToken:string}>("/auth/verify-password-reset-otp",{email:resetFlow.email,otp:code});if(!result.data?.resetToken)throw new Error("Reset proof was not returned");setResetFlow({...resetFlow,resetToken:result.data.resetToken});router.replace("/reset-password");}catch(error){const api=error as ApiError;const next=attempts+1;setAttempts(next);if(api.code==="INVALID_OTP"||api.code==="OTP_EXPIRED")void trackCustomerEvent("OTP Verification Failed",{otp_context:"forgot_password",attempt_number:next,failure_reason:api.code==="OTP_EXPIRED"?"expired":"invalid"});setMessage(errorMessage(error,"Unable to verify this code."));setDigits(["","","","","",""]);refs.current[0]?.focus();}finally{setBusy(false);}};const resend=async()=>{if(!resetFlow||remaining||busy)return;setBusy(true);try{const result=await post<{resendAvailableIn:number}>("/auth/forgot-password",{email:resetFlow.email});const seconds=result.data?.resendAvailableIn??60;const next=resends+1;void trackCustomerEvent("OTP Resend Requested",{otp_context:"forgot_password",resend_count:next});setResends(next);setRemaining(seconds);setResetFlow({...resetFlow,resendAvailableIn:seconds});setMessage("If an account exists, a new code has been sent.");}catch(error){setMessage(errorMessage(error,"Unable to send another code."));}finally{setBusy(false);}};const change=(value:string,index:number)=>{const next=[...digits];next[index]=value.replace(/\D/g,"").slice(-1);setDigits(next);if(next.join("").length===6&&!next.includes(""))void submit(next.join(""));};return <Screen><BrandHeader/><Heading title="Check your email for the password reset code" copy="Enter the six-digit code we sent to your email address."/><View style={styles.otpRow}>{digits.map((digit,index)=><TextInput key={index} ref={node=>{refs.current[index]=node;}} value={digit} onChangeText={value=>change(value,index)} keyboardType="number-pad" maxLength={1} style={styles.box}/>)}</View><ActivityIndicator animating={busy} color={colors.brown}/><FormError>{message}</FormError><PrimaryButton title={remaining?`Resend code in 0:${String(remaining).padStart(2,"0")}`:"Resend code"} disabled={Boolean(remaining)||busy} onPress={resend}/></Screen>}

export function ResetPasswordScreen(){const router=useRouter();const{resetFlow,setResetFlow}=useResetFlow();const{clearSession}=useCustomerSession();const{control,handleSubmit,watch,formState:{errors,isSubmitting},setError}=useForm<ResetPasswordValues>({resolver:zodResolver(resetPasswordSchema),defaultValues:{newPassword:"",confirmPassword:""},mode:"onChange"});const password=watch("newPassword","");useEffect(()=>{if(!resetFlow?.resetToken)router.replace("/forgot-password");},[resetFlow,router]);const submit=async(values:ResetPasswordValues)=>{if(!resetFlow?.resetToken)return;try{await post("/auth/reset-password",{resetToken:resetFlow.resetToken,newPassword:values.newPassword,confirmPassword:values.confirmPassword});await clearSession();setResetFlow(null);router.replace("/login");}catch(error){setError("root",{message:errorMessage(error,"Unable to reset your password.")});}};return <Screen><BrandHeader/><Heading title="Set a new password" copy="Choose a strong password you will remember."/><Controller control={control} name="newPassword" render={({field})=><TextField label="New password" secure value={field.value} onChangeText={field.onChange} error={errors.newPassword?.message}/>}/><PasswordChecklist value={password}/><Controller control={control} name="confirmPassword" render={({field})=><TextField label="Confirm new password" secure value={field.value} onChangeText={field.onChange} error={errors.confirmPassword?.message}/>}/><FormError>{errors.root?.message}</FormError><PrimaryButton title="Save new password" loading={isSubmitting} onPress={handleSubmit(submit)}/></Screen>}
const styles=StyleSheet.create({link:{color:colors.brown,fontWeight:"800",fontSize:13,marginBottom:4},otpRow:{flexDirection:"row",gap:8},box:{flex:1,height:52,backgroundColor:colors.inputBackground,borderRadius:radii.control,textAlign:"center",fontSize:21,fontWeight:"800",color:colors.inputText,borderWidth:1,borderColor:colors.inputBorder}});
