import { describe, expect, it } from "vitest"; import { swaggerSpec } from "./swagger";
describe("marketplace draft Swagger",()=>{it("documents draft and media routes with the actual multipart contract",()=>{const paths=(swaggerSpec as any).paths as Record<string,any>;for(const key of ["/marketplace/seller/properties","/marketplace/seller/properties/{propertyId}","/marketplace/seller/properties/{propertyId}/images","/marketplace/seller/properties/{propertyId}/images/order","/marketplace/seller/properties/{propertyId}/images/{imageId}/cover"])expect(paths[key]).toBeTruthy();const upload=paths["/marketplace/seller/properties/{propertyId}/images"].post;expect(upload.security).toEqual([{bearerAuth:[]}]);expect(upload.requestBody.content["multipart/form-data"].schema.properties.images).toBeTruthy();expect(upload.description).toMatch(/5MB.*10 property photos.*first image becomes cover/i);expect(paths["/marketplace/seller/properties/{propertyId}/images/order"].patch.requestBody).toBeTruthy();expect(Object.keys(paths)).toHaveLength(127)})});
