import type { ApiEnvelope } from "./contracts";
export async function requestApi<T>(path: string, body: unknown, method: "POST" | "PATCH" = "POST", headers?: HeadersInit): Promise<ApiEnvelope<T>> { const response = await fetch(path, { method, headers: { "content-type": "application/json", ...headers }, body: JSON.stringify(body) }); const payload = await response.json().catch(() => ({ success: false, message: "Unexpected response", code: "UPSTREAM_INVALID_RESPONSE" })); if (!response.ok) throw payload as ApiEnvelope<T>; return payload as ApiEnvelope<T>; }
export const postApi = <T>(path: string, body: unknown, headers?: HeadersInit) => requestApi<T>(path, body, "POST", headers);
export const errorMessage = (error: unknown, fallback: string) => { const api = error as ApiEnvelope<unknown>; return api?.message || fallback; };
