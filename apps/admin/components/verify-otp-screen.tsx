"use client";
import { useRouter } from "next/navigation";
import { useEffect, useMemo, useState } from "react";
import { errorMessage, postApi } from "@/lib/client-api";
import type { LoginChallenge } from "@/lib/contracts";
import { AuthLayout } from "./auth-layout";
import { OtpInput } from "./otp-input";
type ChallengeState = Pick<LoginChallenge, "challengeId" | "maskedEmail" | "resendAvailableIn">;
const readChallenge = (): ChallengeState | null => { try { return JSON.parse(sessionStorage.getItem("beryl_admin_login_challenge") ?? "") as ChallengeState; } catch { return null; } };
export function VerifyOtpScreen() { const router = useRouter(); const [challenge] = useState<ChallengeState | null>(() => typeof window === "undefined" ? null : readChallenge()); const [remaining, setRemaining] = useState(() => typeof window === "undefined" ? 0 : readChallenge()?.resendAvailableIn ?? 0); const [message, setMessage] = useState(""); const [busy, setBusy] = useState(false); const [resetKey, setResetKey] = useState(0); useEffect(() => { if (!challenge) router.replace("/login"); }, [challenge, router]); useEffect(() => { if (!remaining) return; const timer = window.setTimeout(() => setRemaining((value) => Math.max(0, value - 1)), 1000); return () => window.clearTimeout(timer); }, [remaining]); const formatted = useMemo(() => `${Math.floor(remaining / 60)}:${String(remaining % 60).padStart(2, "0")}`, [remaining]); const verify = async (otp: string) => { if (!challenge || busy) return; setBusy(true); setMessage(""); try { const response = await postApi<{ nextAction: string }>("/api/admin/verify-login-otp", { challengeId: challenge.challengeId, otp }); sessionStorage.removeItem("beryl_admin_login_challenge"); router.replace(response.data?.nextAction === "CHANGE_INITIAL_ADMIN_PASSWORD" ? "/change-initial-password" : "/dashboard"); } catch (error) { const code = (error as { code?: string }).code; const copy: Record<string, string> = { INVALID_OTP: "That code is not correct. Please try again.", OTP_EXPIRED: "This code has expired. Request a new one.", OTP_ATTEMPTS_EXCEEDED: "Too many attempts. Request a new code.", OTP_NO_LONGER_VALID: "This code is no longer valid. Request a new one.", ADMIN_LOGIN_CHALLENGE_INVALID: "Your login challenge is no longer valid. Sign in again." }; setMessage(copy[code ?? ""] ?? errorMessage(error, "Unable to verify this code.")); setResetKey((value) => value + 1); } finally { setBusy(false); } }; const resend = async () => { if (!challenge || remaining || busy) return; setBusy(true); setMessage(""); try { const response = await postApi<{ resendAvailableIn: number }>("/api/admin/resend-login-otp", { challengeId: challenge.challengeId }); const next = response.data?.resendAvailableIn ?? 60; setRemaining(next); sessionStorage.setItem("beryl_admin_login_challenge", JSON.stringify({ ...challenge, resendAvailableIn: next })); setMessage("A new verification code has been sent."); setResetKey((value) => value + 1); } catch (error) { setMessage(errorMessage(error, "Unable to resend a code.")); } finally { setBusy(false); } }; return <AuthLayout title="A protected sign-in, every time."><p className="eyebrow">Admin verification</p><h1 className="page-title">Enter verification code</h1><p className="page-copy">We sent a six-digit code to <strong>{challenge?.maskedEmail ?? "your email"}</strong>.</p><OtpInput key={resetKey} onComplete={verify} disabled={busy} /><div aria-live="polite" style={{ minHeight: 44, marginTop: 16 }}>{message ? <p className={message.startsWith("A new") ? "alert alert-success" : "alert alert-error"} role="alert">{message}</p> : null}</div><button className="button button-primary" style={{ width: "100%", marginTop: 12 }} type="button" disabled={busy || remaining > 0} onClick={resend}>{remaining ? `Resend code in ${formatted}` : "Resend code"}</button><button className="icon-btn" style={{ marginTop: 16, color: "var(--color-admin-brown)", fontWeight: 700 }} type="button" onClick={() => { sessionStorage.removeItem("beryl_admin_login_challenge"); router.push("/login"); }}>Use a different account</button></AuthLayout>; }
